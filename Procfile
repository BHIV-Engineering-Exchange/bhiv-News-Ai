# Insight Node - Render Deployment Config
# Build Command: pip install -r requirements.txt
# Start Command: Change to the backend directory and run uvicorn
# We use 'cd' to switch directory, then run uvicorn pointing to main:app
web: cd unified_tools_backend && uvicorn main:app --host 0.0.0.0 --port $PORT
