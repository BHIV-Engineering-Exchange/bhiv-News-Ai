# Insight Node - Render Deployment Config
# Build Command: pip install -r requirements.txt
# Start Command: uvicorn unified_tools_backend.main:app --host 0.0.0.0 --port $PORT

web: uvicorn unified_tools_backend.main:app --host 0.0.0.0 --port $PORT
